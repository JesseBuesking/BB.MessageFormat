#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  "$basedir/coffee"  "$basedir/../watchr/bin/watchr" "$@"
  ret=$?
else 
  coffee  "$basedir/../watchr/bin/watchr" "$@"
  ret=$?
fi
exit $ret
